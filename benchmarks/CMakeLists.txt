# Placeholder. There is nothing to measure until the cache engine exists.
#
# Built only when -DCACHEX_BUILD_BENCHMARKS=ON, so the default build stays fast.
# See benchmarks/README.md for what will live here.
message(STATUS "CacheX benchmarks: no benchmark targets yet")
