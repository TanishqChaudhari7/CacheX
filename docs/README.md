# docs/

Longer-form notes that would otherwise bloat `ARCHITECTURE.md`.

`ARCHITECTURE.md` stays at the repository root and answers *what the system is
and why*. This directory is for the supporting detail: benchmark write-ups,
protocol specifications, and notes on designs that were considered and rejected.

Nothing here yet — the project is at the foundation stage.
