{
  "version": 4,
  "configurations": [
    {
      "name": "CacheX",
      "//": "compile_commands.json is symlinked into the source root by CMake, so these settings follow the real build flags. The includePath and cppStandard below are only a fallback for files the compile database does not cover.",
      "compileCommands": "${workspaceFolder}/compile_commands.json",
      "includePath": [
        "${workspaceFolder}/include",
        "${workspaceFolder}/build/debug/generated",
        "${workspaceFolder}/build/release/generated"
      ],
      "cStandard": "c17",
      "cppStandard": "c++17",
      "intelliSenseMode": "macos-clang-arm64"
    }
  ]
}
